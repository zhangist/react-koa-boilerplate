import React from 'react';
import translate from '../../common/translate';

export default () => (
  <div className="page site-explore">
    <div className="page-title">{translate('Default Page')}</div>
    <div className="page-subtitle">{translate('Default Page Sub-title.')}</div>
  </div>
);
